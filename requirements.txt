# Broker runtime deps. FastMCP (in `mcp`) brings Starlette + Uvicorn for the
# streamable-HTTP transport; uvicorn is listed explicitly to be safe.
# FastMCP moved in mcp 2.x; broker.py currently targets the 1.x API.
mcp>=1.2.0,<2
httpx>=0.27
uvicorn>=0.30
